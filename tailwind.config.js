/** @type {import('tailwindcss').Config} */
export default {
  content: [
    './index.html' // Targets the index.html file at the root of your project directory
  ],
  darkMode: 'class', // Ensure dark mode is controlled by the 'dark' class
  theme: {
    extend: {},
  },
  plugins: [],
}
